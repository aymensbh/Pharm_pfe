class Patient {
  int id;
  String fullname, doctor, birthdate, phone, addess;
  num sc;

  Patient(
      {this.id,
      this.fullname,
      this.birthdate,
      this.sc,
      this.phone,
      this.addess});
}
